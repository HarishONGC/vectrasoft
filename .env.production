# Root environment file for Docker Compose deployment
# Copy to the VM and set values before running docker-compose up

# Database
DB_PASSWORD=securedatabase
DB_NAME=vms

# CORS - for production, set to your domain only
# Example: https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGIN=https://vectrasoft.onrender.com/

# Frontend API & WebSocket URLs (must match your public domain/IP)
# Example: https://yourdomain.com or http://203.0.113.45
VITE_API_BASE_URL=/api
VITE_WS_URL=https://vectrasoft.onrender.com/
